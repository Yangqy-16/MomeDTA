_BASE_: fusionnet.yaml

MODEL:
  NAME: MoE

  DRUG_2D_MODEL: null
  DRUG_2D_DIM: null
  PROT_2D_MODEL: null
  PROT_2D_DIM: null

  FUSION: MANnew

MODULE:
  SCHEDULER:
    NAME: ReduceLROnPlateau

    # ReduceLROnPlateau
    FACTOR: 0.2
    PATIENCE: 60

TRAINER:
  MAX_EPOCHS: 2000        # If profiler is enabled, this will be *automatically* set to 1
  LOG_EVERY_N_STEPS: 200

  PATIENCE: 100
